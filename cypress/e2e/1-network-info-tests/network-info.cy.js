describe('Network Info Tracker', () => {
  it('should load and show the heading', () => {
    cy.visit('/'); // Replace with your local dev URL
    cy.contains('Network Information Tracker');
  });

  
});
